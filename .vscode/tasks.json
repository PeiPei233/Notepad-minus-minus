{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "build",
			"command": "mingw32-make",
			"args": [
				// "-f",
				// "${workspaceFolder}/makefile",
				// "all",
				// "-I",
				// "${workspaceFolder}\\libgraphics"
				// "-l",
				// "${workspaceFolder}\\libgraphics",
				// "taget=${workspaceFolder}.exe"
			],
			"problemMatcher": []
		},
    ]
}